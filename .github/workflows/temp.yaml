name: Custom shell

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
      - name: Display the path
        run: |
          echo {{ date -v+1w '+%V, %Y' }}
          echo date -v+1w '+%V, %Y'

