name: Python package

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
#     - uses: actions/checkout@v2
#     - uses: shogo82148/actions-setup-perl@v1
#       with:
#         perl-version: '5.32'
    - run: |
          print %ENV
          shell: perl {0}

