name: print pr context

on:
  pull_request:
    types: [labeled]

jobs:
 print context:
   runs-on: ubuntu-latest
   steps:
   - name: print
     env:
      GITHUB_CONTEXT: ${{ toJSON(github) }}
     run: echo "$GITHUB_CONTEXT"
