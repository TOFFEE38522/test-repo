name: vars
on: [push, workflow_dispatch]
env: 
  contvar: true
  timevar: 3
  
# jobs:
#   with-job:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - id: foo
#         uses: skedwards88/test-area@v5
#         with:
#           who-to-greet: ${{ env.flowvar }} 
    
jobs:
  fail:
    runs-on: ubuntu-latest
    steps: 
      - continue-on-error: ${{ fromJson(env.contvar) }}
        timeout-minutes: ${{ fromJson(env.timevar) }}
        run: |
          echo hhh
          exit 1
          echo kre
  conttt:
    runs-on: ubuntu-latest
    needs: [fail]
    steps: 
      - run: |
          echo mmmm

   
#   ${{ env.flowvar }}:
#     steps:
#       - name: ${{ env.flowvar }}
#         run: echo "${{ env.jobvar }}"
#     runs-on: ubuntu-latest
#     env: 
#       jobvar: c

     
  
#   echo-job-var:
#     steps:
#       - name: echo-step
#         run: echo "${{ env.jobvar }}"
#     runs-on: ubuntu-latest
#     env: 
#       jobvar: jobvar

#   echo-step-var:
#     steps:
#       - name: echo-step
#         run: echo "${{ env.stepvar1 }}"
#         env:
#           stepvar1: stepvar1  
#     runs-on: ubuntu-latest
    
#   conditional-job:
#     steps:
#      - name: conditional=step
#        if: ${{ env.var1 == 'cow' }}
#        run: echo "${{ env.var1 }}"
#     runs-on: ubuntu-latest
#     env: 
#       var1: cow
